events {}

http {
  include mime.types;

  server {
    listen 80;

    location /api {
      proxy_pass http://backend:5050;
    }

    location / {
      proxy_pass http://frontend:3000;
    }
  }
}